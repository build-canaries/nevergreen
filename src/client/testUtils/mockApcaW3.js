/*
 * apca-w3 is a pure esm module, which doesn't work "out the box" with Jest.
 * I tried following several guides online but couldn't get it working.
 * TODO: remove this file and configure babel/jest correctly
 */
module.exports = {
  calcAPCA() {
    return 0
  },
}
