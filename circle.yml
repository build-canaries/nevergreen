machine:
  node:
    version: 4.1.0
  java:
    version: openjdk7

general:
    artifacts:
        - "target/nevergreen-standalone.jar"

test:
    pre:
        - npm run sass
    override:
        - npm test
        - npm run lint
        - lein midje 'nevergreen.*'
